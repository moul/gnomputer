{
  "name": "@gnomputer/lenses",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gnomputer/entities": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.7",
    "typescript": "^5.7.0"
  }
}
