{
  "name": "@gnomputer/trails",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gnomputer/storage": "workspace:*"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "vitest": "^2.1.8",
    "typescript": "^5.7.0"
  }
}
